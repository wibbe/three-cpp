
#include "image.h"
#include "loader/StbImage.cpp"

namespace three {
  
  namespace image {

    Image * load(foundation::Allocator & allocator, const char * filename)
    {
      return 0;
    }

  }

}